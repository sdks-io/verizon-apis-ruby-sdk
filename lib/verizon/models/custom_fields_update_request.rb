# verizon
#
# This file was automatically generated by APIMATIC v2.0
# ( https://apimatic.io ).

module Verizon
  # Request to assign or change custom field values for one or more devices.
  class CustomFieldsUpdateRequest < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # The name of a billing account.This parameter is only required if the UWS
    # account used for the current API session has access to multiple billing
    # accounts.An account name is usually numeric, and must include any leading
    # zeros.
    # @return [String]
    attr_accessor :account_name

    # Custom field names and values, if you want to only include devices that
    # have matching values.
    # @return [Array[CustomFields]]
    attr_accessor :custom_fields

    # The names and new values of any custom fields that you want to change.
    # @return [Array[CustomFields]]
    attr_accessor :custom_fields_to_update

    # The devices that you want to change.
    # @return [Array[AccountDeviceList]]
    attr_accessor :devices

    # The name of a device group, if you want to only include devices in that
    # group.
    # @return [String]
    attr_accessor :group_name

    # The name of a service plan, if you want to only include devices that have
    # that service plan.
    # @return [String]
    attr_accessor :service_plan

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['account_name'] = 'accountName'
      @_hash['custom_fields'] = 'customFields'
      @_hash['custom_fields_to_update'] = 'customFieldsToUpdate'
      @_hash['devices'] = 'devices'
      @_hash['group_name'] = 'groupName'
      @_hash['service_plan'] = 'servicePlan'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      %w[
        account_name
        custom_fields
        custom_fields_to_update
        devices
        group_name
        service_plan
      ]
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(account_name = SKIP,
                   custom_fields = SKIP,
                   custom_fields_to_update = SKIP,
                   devices = SKIP,
                   group_name = SKIP,
                   service_plan = SKIP)
      @account_name = account_name unless account_name == SKIP
      @custom_fields = custom_fields unless custom_fields == SKIP
      @custom_fields_to_update = custom_fields_to_update unless custom_fields_to_update == SKIP
      @devices = devices unless devices == SKIP
      @group_name = group_name unless group_name == SKIP
      @service_plan = service_plan unless service_plan == SKIP
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      account_name = hash.key?('accountName') ? hash['accountName'] : SKIP
      # Parameter is an array, so we need to iterate through it
      custom_fields = nil
      unless hash['customFields'].nil?
        custom_fields = []
        hash['customFields'].each do |structure|
          custom_fields << (CustomFields.from_hash(structure) if structure)
        end
      end

      custom_fields = SKIP unless hash.key?('customFields')
      # Parameter is an array, so we need to iterate through it
      custom_fields_to_update = nil
      unless hash['customFieldsToUpdate'].nil?
        custom_fields_to_update = []
        hash['customFieldsToUpdate'].each do |structure|
          custom_fields_to_update << (CustomFields.from_hash(structure) if structure)
        end
      end

      custom_fields_to_update = SKIP unless hash.key?('customFieldsToUpdate')
      # Parameter is an array, so we need to iterate through it
      devices = nil
      unless hash['devices'].nil?
        devices = []
        hash['devices'].each do |structure|
          devices << (AccountDeviceList.from_hash(structure) if structure)
        end
      end

      devices = SKIP unless hash.key?('devices')
      group_name = hash.key?('groupName') ? hash['groupName'] : SKIP
      service_plan = hash.key?('servicePlan') ? hash['servicePlan'] : SKIP

      # Create object from extracted values.
      CustomFieldsUpdateRequest.new(account_name,
                                    custom_fields,
                                    custom_fields_to_update,
                                    devices,
                                    group_name,
                                    service_plan)
    end
  end
end
